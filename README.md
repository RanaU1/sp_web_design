# Rana Universe Website

## How i am making this?
I will try to do changes in the `dev` branch and after i will ready to make changes i will merge this to the `main` branch.

## How i will use git
This below will make one branch here.
```
git checkout -b dev
```
This below is for adding things in the website:
```
git add .
git commit -m "testing and add thigns in the dev branch
git push origin dev
```
When i will sure i need to merge this to main i will:
```
git checkout main
git merge dev
git push origin main
```
For this upper case i can go to github website and do the same.